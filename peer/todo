Create local drive interface which can:
-save and load the config
	-local peer id
-save and load encrypted local files

Create a network adapter for peers:
-connect to a non firewalled peer
-connect using a hole punch

Auth server connection:
-get addresses of other peers
